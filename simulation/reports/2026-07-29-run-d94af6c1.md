# AetherCal — two-week load simulation, run `d94af6c1`

**Verdict: MEASURED.** Every control held, so the numbers below were taken by an instrument that was demonstrably working.

- Seed `20260725` — the plan is deterministic: same seed, same load
- Started `2026-07-29T23:53:56+00:00` · finished `2026-07-29T23:58:52+00:00`
- Host: aethercal-sim-throwaway · Linux 7.0.14-3-pve · python 3.13.5
- Stack: throwaway docker compose project `aethercal-sim` (deploy/docker-compose.yml + e2e/compose.e2e.yml + simulation/compose.sim.yml)
- Organic concurrency: 8 simultaneous guests · adversarial burst: 40 contenders

## 1. Volume — what two simulated weeks looked like

- Planned bookings: **240**, over **10** weekdays
- By business: `{'clinica-sonrisa': 80, 'katy-hvac': 80, 'estudio-legal': 80}`
- By locale: `{'es': 130, 'en': 110}`
- By event type: `{'standard': 186, 'long': 54}`
- By weekday (0=Mon): `{'0': 60, '1': 54, '2': 48, '3': 48, '4': 30}` — **no weekend entries: the organic schedule is Mon-Fri, and a closed day offers nothing to book**

- Bookings **created**: **210**
- Cancelled: **21** · rescheduled: **20**
- Organic collisions on the CREATE leg (two guests, one slot → `slot_unavailable`): **30**
- Organic collisions on the RESCHEDULE leg (the target slot was taken between reading the offer and posting the move): **2**
- Attempts that met a fully-booked day (an empty offer from a **well-formed 2xx**): 0

**Every planned intent, accounted for** — the categories must sum to what was planned:

| leg | outcome | n |
|---|---|---:|
| create | `booked` | 210 |
| create | `collisions` | 30 |
| create | `no_slots_offered` | 0 |
| create | `slots_read_failed` | 0 |
| create | `booking_refused` | 0 |
| create | `booking_unreadable` | 0 |
| follow-up | `cancelled` | 21 |
| follow-up | `cancel_refused` | 0 |
| follow-up | `rescheduled` | 20 |
| follow-up | `reschedule_collisions` | 2 |
| follow-up | `reschedule_refused` | 0 |
| follow-up | `reschedule_no_target` | 0 |
| follow-up | `reschedule_slots_read_failed` | 0 |

Create outcomes sum to **240** of **240** planned · follow-up outcomes sum to **43** of **43** attempted. **C13** is the control on that arithmetic.

## 2. Latency (milliseconds, measured client-side, nearest-rank percentiles)

| path | n | min | p50 | p95 | p99 | max |
|---|---:|---:|---:|---:|---:|---:|
| `slots_read` | 262 | 7.2 | 79.0 | 278.3 | 376.1 | 543.0 |
| `booking_create` | 240 | 17.0 | 83.1 | 264.6 | 392.9 | 441.7 |
| `booking_cancel` | 21 | 18.0 | 89.1 | 234.9 | 255.1 | 255.1 |
| `booking_reschedule` | 22 | 23.4 | 88.3 | 182.5 | 333.6 | 333.6 |
| `race_same_slot` | 40 | 159.8 | 313.2 | 434.3 | 447.1 | 447.1 |
| `control_race_distinct_slots` | 40 | 112.8 | 557.3 | 992.5 | 1044.6 | 1044.6 |
| `race_cancel_same_booking` | 40 | 114.9 | 323.7 | 524.0 | 548.7 | 548.7 |
| `race_reschedule_same_booking` | 40 | 116.5 | 194.9 | 250.8 | 255.7 | 255.7 |
| `race_cancel_vs_reschedule` | 2 | 16.6 | 16.6 | 26.4 | 26.4 | 26.4 |
| `booking_to_confirmation_email` | 185 | 3426.8 | 4463.0 | 6045.3 | 7462.2 | 7471.3 |

`slots_read` covers **both** legs of the organic phase — the read a guest makes before booking and the one before a reschedule. The second reached neither instrument, so this row counted only the create leg and §6 was short by one response per reschedule attempted, while calling itself *every outcome*.

`booking_create` is the guest's own wait for a 201. `booking_to_confirmation_email` is a **different and slower** question: it crosses the outbox and is produced by the worker, not by the request that returned 201.

==`booking_to_confirmation_email` is measured from the instant the POST was **sent**, not the instant it returned.== It used to be the latter, which made the reference later than an event it is supposed to precede: the worker can send the mail while the guest's own response is still in flight. Those confirmations produced a negative delta and were dropped by a bare `if delta_ms >= 0`. Only the FASTEST can precede their own POST, so the discard removed exactly the left tail and every percentile in this row moved UP. Negative deltas in this run: **0** — and a non-zero count is now clock skew, reported and gating (**C14**), never a silent filter.

## 3. The outbox and the drain

- Backlog samples taken: **505** (scrape failures: **0**)
- Reads discarded at the deliberate-outage boundary (C5's pause): **0**
- **Peak `due` backlog: 176**
- **Peak oldest-due age: 8.0s**
- Drained to zero at the end: **yes** (waited 0.0s)
- Dead-man control figures: `{'work_created': 6.0, 'baseline_due': 0.0, 'surface_unreachable_during_outage': 1.0, 'outbox_rows_baseline': 578.0, 'outbox_rows_final': 590.0, 'delivered_after_restart': 6.0, 'backlog_caught_in_flight': 1.0, 'first_reading_after_restart': 6.0, 'first_reading_oldest_age_seconds': 29.972091, 'drain_recovery_seconds': 7.851462404709309}`
- Messages in the mailbox: **279** as Mailpit reports them · **279** read back over **1** page(s) of 500 (unreadable envelopes: 0)
- Mailbox read **complete: yes**
- Confirmations matched to a created booking: **185 of 210** (in 3 read(s), 7.6s) · confirmations that PRECEDED their own POST: **0**
- Webhook deliveries captured at the sink, by event: `{'booking.created': 263, 'booking.cancelled': 23, 'booking.rescheduled': 21}` (unreadable payloads: 0)

## 4. Adversarial concurrency — the part real traffic never produces

Each burst is released by a `threading.Barrier`: N threads block until all N are ready, then fire together. Two weeks of organic bookings would never collide like this.

| race | contenders | winners | refusals | by code | peak in flight |
|---|---:|---:|---:|---|---:|
| `race_same_slot` | 40 | **1** | 39 | `{'slot_unavailable': 39}` | **40** |
| `control_race_distinct_slots` | 40 | **40** | 0 | `{}` | **40** |
| `race_cancel_same_booking` | 40 | **40** | 0 | `{}` | **40** |
| `race_reschedule_same_booking` | 40 | **1** | 39 | `{'not_active': 39}` | **40** |
| `race_cancel_vs_reschedule` | 2 | **1** | 1 | `{'not_active': 1}` | **2** |

`peak in flight` is the largest number of that race's requests open at the same instant, measured from per-request start/end timestamps on one monotonic clock. A serialised harness peaks at **1**; **C15** requires at least 2, and the winner counts above are evidence of nothing without it.

- No-show transition (against a real end time, not a clock trick): **200 ok → status=no_show**

## 5. Controls — why any of the above is believable

==A control that comes back green when it should have failed voids the run.== These are the cases that MUST be refused; without them the numbers above are anecdote.

Required control set: **15** ids (`C1, C2, C3, C4, C5, C6, C7, C8, C9, C10, C11, C12, C13, C14, C15`). Accounted for in this run: **15**.

| id | guards | expected | observed | held |
|---|---|---|---|:--:|
| **C1** | the no-double-booking claim: an already-taken slot is refused | 409 slot_unavailable | 409 slot_unavailable | ✅ |
| **C2** | the race oracle itself: it CAN see more than one winner | 40 winners on 40 distinct slots | 40 winners, refusals {} | ✅ |
| **C3** | the schedule is enforced: a closed weekend offers no slots | 0 slots offered on a Saturday, from a well-formed 2xx | 0 slots offered (availability='ok') | ✅ |
| **C4** | the daily cap bites (max_per_day=2) | two created, the third refused (day_full) or no longer offered | created(201) → created(201) → no_slots_offered | ✅ |
| **C5** | the backlog metric is LIVE (stranded work is seen, and then cleared) | the operator surface goes unreachable with the worker; the DB-derived snapshot and the restarted worker's delivered counter both account for the stranded work; the backlog then returns to 0 | stranded 6 booking(s); surface unreachable during the outage: yes; outbox rows 578→590 (+12); restarted worker delivered=6; backlog caught in flight: yes (first reading due=6, oldest 30.0s); drained in 7.9s | ✅ |
| **C6** | the no-show guard: an appointment that has not ended cannot be a no-show | 409 not_ended | 409 not_ended | ✅ |
| **C7** | an idempotent cancel emits exactly ONE booking.cancelled webhook | exactly 1 booking.cancelled at the sink for this booking | exactly 1 booking.cancelled delivered · all events {'booking.created': 1, 'booking.cancelled': 1} · unreadable deliveries 0 · drained in 8.1s, then polled up to 60s + 10s settling | ✅ |
| **C8** | a reschedule race leaves ONE successor, not two live appointments | exactly 1 winner, and exactly 1 SUCCESSOR (not the original) survives | race winners 1/40; 1 active: ['1f5e7eb7-3f66-4bbd-b0d2-7cbcd851b94f confirmed']; subject 89d53aaf-61f6-47d0-b35b-4a0e4ca7c6be | ✅ |
| **C9** | a cancel racing a reschedule never leaves TWO live appointments | at least 1 winner, and at most 1 active booking — never the original | race winners 1/2; 0 active: none; subject add98998-7359-4e9f-ac5e-dd07a962847f | ✅ |
| **C10** | RF-04 itself: N simultaneous bookings of ONE slot leave exactly one | exactly 1 winner of 40, the rest `slot_unavailable` | 1 winner(s), 39 refused {'slot_unavailable': 39} | ✅ |
| **C11** | the no-show transition itself, against a real end time | 200, and the booking's status really becomes no_show | 200 ok → status=no_show | ✅ |
| **C12** | the outbox actually drained, and the backlog was readable throughout | due == 0 before the run ends, with 0 unexplained scrape failures | drained=True after 0.0s; unexplained scrape failures=0 | ✅ |
| **C13** | the organic phase is fully explained: every planned intent lands in a known outcome, and no request failed unexpectedly | 240 planned = the sum of the create outcomes, 43 follow-ups = the sum of the follow-up outcomes, and 0 unexpected failures | creates 240/240 `{'booked': 210, 'collisions': 30, 'no_slots_offered': 0, 'slots_read_failed': 0, 'booking_refused': 0, 'booking_unreadable': 0}`; follow-ups 43/43 `{'cancelled': 21, 'cancel_refused': 0, 'rescheduled': 20, 'reschedule_collisions': 2, 'reschedule_refused': 0, 'reschedule_no_target': 0, 'reschedule_slots_read_failed': 0}`; unexpected failures: none | ✅ |
| **C14** | the booking→confirmation sample is complete: the mailbox was read whole and every created booking is in it | a complete mailbox read and 210 of 210 bookings matched, with 0 negative deltas | matched 185/210 (superseded 25, unaccounted 0) after 3 read(s) in 7.6s; mailbox read complete=True (reported total 279, page size 500); negative deltas=0; duplicate confirmations=0; uid collisions=0; messages carrying a calendar invite=279 | ✅ |
| **C15** | the adversarial bursts were really SIMULTANEOUS — the barrier worked, rather than being trusted | every multi-contender race has >= 2 requests in flight at the same instant | peak overlap by race: {'race_same_slot': 40, 'control_race_distinct_slots': 40, 'race_cancel_same_booking': 40, 'race_reschedule_same_booking': 40, 'race_cancel_vs_reschedule': 2} | ✅ |

## 6. Error taxonomy (every outcome, successes included)

Total classified responses: **545** · non-2xx: **32**

| status | machine code | count |
|---:|---|---:|
| 200 | `ok` | 303 |
| 201 | `ok` | 210 |
| 409 | `slot_unavailable` | 32 |

## 7. Limits — what this dataset does and does NOT support

### It DOES support these claims

These are **mechanical guarantees**: properties of the code under concurrency. A compressed simulation tests them at least as harshly as calendar time would — harder, in fact, because real traffic never collides on purpose.

- **No double-booking under simultaneous contention.** Barrier-released bursts at one slot left exactly one winner, and the distinct-slot control (C2) proves the oracle could have seen more than one.
- **Mutations are serialised, checked on the DIARY rather than on status codes.** Concurrent cancels of one booking emit exactly one `booking.cancelled` (C7); a reschedule race leaves exactly one successor (C8); a cancel racing a reschedule never leaves two live appointments (C9). ==The winner counts in §4 are not the oracle for the last two==: a cancel and a reschedule may BOTH legitimately answer 200, because the reschedule swaps in a successor and the cancel then finds the predecessor already cancelled and is an idempotent no-op. What must never happen is two live appointments, so that is what is asserted.
- **Latency under this load, on this hardware.** The distributions in §2, at the sample sizes shown there — and, for `booking_to_confirmation_email`, over a sample proven COMPLETE rather than over whatever survived a mailbox ceiling and a non-negative filter (C14).
- **The organic load really was this load.** Every planned intent lands in one explained outcome and the outcomes reconcile against the plan (C13), so §1 cannot be describing a quiet fortnight that was really a failing one.
- **The drain keeps up, and a dead drain would be visible.** Peak backlog in §3, with the dead-man control (C5) proving the metric is live rather than a constant zero — judged on signals that DURABLY account for the stranded work, not on catching an instantaneous gauge before the drain clears it.
- **Both locales and several guest timezones exercise the same paths** without a distinct error class appearing in §6.

### It does NOT support these — by construction, not by omission

- ==**"≥5 bookings from real humans."** That is a fact about ADOPTION, and no volume of synthetic load can stand in for it.== Every booking here was made by this harness against a throwaway database. The rubric's human-uptake criterion is untouched by this run and must still be met by the pilot itself.
- ==**"Two weeks elapsed."** The run compresses two weeks of VOLUME into minutes; it does not compress two weeks of TIME.== Nothing here can see certificate expiry, log-rotation faults, a cron that drifts, a slow memory leak, connection churn over days, a 24h reminder actually firing, or a DST transition. Duration-dependent failure is what a soak test finds, and this is not one.
- **Real-world demand shape.** The weekday weights are plausible, not fitted — five pilot bookings cannot fit a distribution. Read §1 as a load shape, never as a forecast.
- **Real email deliverability.** Mail lands in Mailpit over plaintext SMTP on the same host. This says nothing about SPF/DKIM/DMARC, spam placement, or whether any real inbox would accept the message.
- **Real network conditions.** No reverse proxy, no TLS termination, no WAN latency, no CDN. Client and server share a host, so §2 is a floor for the product's own work — not what a guest in another city experiences.
- **The booking PAGE.** The harness drives the API. Front-end budgets (LCP/CLS/INP) and the browser journey are measured elsewhere (`e2e/`, the CWV guard) and are out of scope.
- **External calendar integrations.** No Google or CalDAV connection is configured, so the busy-cache refresh path and its degraded/unavailable modes are not exercised.
- **Payments.** No payment provider is configured; the money paths are untouched.
- **Multi-worker safety.** The stack runs the shipped `replicas: 1`. This says nothing about what two workers would do — which the product documents as unsafe by design.
- ==**A DEAD worker is not visible in these numbers.**== `/metrics/summary` is served BY the worker, so while the drain is down the backlog does not climb — the endpoint is unreachable (`Connection refused`). C5 confirms that directly. The backlog metric therefore detects a drain that is FALLING BEHIND, not one that is GONE; the second needs external liveness monitoring, and nothing in §3 substitutes for it.

### Honest reading of the sample sizes

The p99 figures in §2 rest on the counts in that table. A p99 over 240 observations is one or two slow requests, not a stable tail estimate; p50 and p95 are the trustworthy columns at this volume.

> [!danger] Drain latency has **185** samples for **210** bookings, so §2's `booking_to_confirmation_email` row describes a SUBSET. ==This used to be a warning and nothing else, which is how a run shipped MEASURED while its headline latency quietly described whatever had survived.== It is **C14** now: a gap here voids the run rather than annotating it.
