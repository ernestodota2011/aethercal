# AetherCal — two-week load simulation, run `b72197a2`

**Verdict: MEASURED.** Every control held, so the numbers below were taken by an instrument that was demonstrably working.

- Seed `20260725` — the plan is deterministic: same seed, same load
- Started `2026-07-29T01:57:24+00:00` · finished `2026-07-29T02:00:41+00:00`
- Host: aethercal-sim-throwaway · Linux 6.17.2-2-pve · python 3.13.5
- Stack: throwaway docker compose project `aethercal-sim` (deploy/docker-compose.yml + e2e/compose.e2e.yml + simulation/compose.sim.yml)
- Organic concurrency: 8 simultaneous guests · adversarial burst: 40 contenders

## 1. Volume — what two simulated weeks looked like

- Planned bookings: **239**, over **10** weekdays
- By business: `{'clinica-sonrisa': 81, 'katy-hvac': 79, 'estudio-legal': 79}`
- By locale: `{'es': 119, 'en': 120}`
- By event type: `{'standard': 187, 'long': 52}`
- By weekday (0=Mon): `{'0': 57, '1': 55, '2': 48, '3': 46, '4': 33}` — **no weekend entries: the organic schedule is Mon-Fri, and a closed day offers nothing to book**

- Bookings **created**: **207**
- Cancelled: **22** · rescheduled: **17**
- Organic collisions (two guests, one slot → `slot_unavailable`): **32**
- Attempts that met a fully-booked day (no slots offered): 0

## 2. Latency (milliseconds, measured client-side, nearest-rank percentiles)

| path | n | min | p50 | p95 | p99 | max |
|---|---:|---:|---:|---:|---:|---:|
| `slots_read` | 239 | 7.1 | 68.0 | 231.6 | 328.5 | 402.1 |
| `booking_create` | 239 | 17.7 | 85.2 | 270.7 | 444.1 | 574.9 |
| `booking_cancel` | 22 | 30.0 | 81.5 | 244.8 | 265.5 | 265.5 |
| `booking_reschedule` | 20 | 45.9 | 89.8 | 285.0 | 318.8 | 318.8 |
| `race_same_slot` | 40 | 193.9 | 336.6 | 440.6 | 457.7 | 457.7 |
| `control_race_distinct_slots` | 40 | 89.8 | 517.1 | 927.7 | 975.1 | 975.1 |
| `race_cancel_same_booking` | 40 | 102.6 | 253.6 | 379.6 | 391.0 | 391.0 |
| `race_reschedule_same_booking` | 40 | 99.8 | 173.7 | 229.7 | 236.5 | 236.5 |
| `race_cancel_vs_reschedule` | 2 | 16.0 | 16.0 | 21.7 | 21.7 | 21.7 |
| `booking_to_confirmation_email` | 207 | 3168.7 | 5493.8 | 10205.9 | 11325.0 | 11486.1 |

`booking_create` is the guest's own wait for a 201. `booking_to_confirmation_email` is a **different and slower** question: it crosses the outbox and is produced by the worker, not by the request that returned 201.

## 3. The outbox and the drain

- Backlog samples taken: **316** (scrape failures: **0**)
- **Peak `due` backlog: 182**
- **Peak oldest-due age: 7.8s**
- Drained to zero at the end: **yes** (waited 0.0s)
- Dead-man control figures: `{'baseline_due': 0.0, 'surface_unreachable_during_outage': 1.0, 'first_reading_after_restart': 6.0, 'first_reading_oldest_age_seconds': 30.891183, 'drain_recovery_seconds': 7.343914688972291}`
- Confirmation emails in the mailbox: **273** (unreadable timestamps: 0)
- Webhook deliveries captured at the sink, by event: `{'booking.created': 260, 'booking.cancelled': 24, 'booking.rescheduled': 18}` (unreadable payloads: 0)

## 4. Adversarial concurrency — the part real traffic never produces

Each burst is released by a `threading.Barrier`: N threads block until all N are ready, then fire together. Two weeks of organic bookings would never collide like this.

| race | contenders | winners | refusals | by code |
|---|---:|---:|---:|---|
| `race_same_slot` | 40 | **1** | 39 | `{'slot_unavailable': 39}` |
| `control_race_distinct_slots` | 40 | **40** | 0 | `{}` |
| `race_cancel_same_booking` | 40 | **40** | 0 | `{}` |
| `race_reschedule_same_booking` | 40 | **1** | 39 | `{'not_active': 39}` |
| `race_cancel_vs_reschedule` | 2 | **1** | 1 | `{'not_active': 1}` |

- No-show transition (against a real end time, not a clock trick): **200 ok → status=no_show**

## 5. Controls — why any of the above is believable

==A control that comes back green when it should have failed voids the run.== These are the cases that MUST be refused; without them the numbers above are anecdote.

Required control set: **12** ids (`C1, C2, C3, C4, C5, C6, C7, C8, C9, C10, C11, C12`). Accounted for in this run: **12**.

| id | guards | expected | observed | held |
|---|---|---|---|:--:|
| **C1** | the no-double-booking claim: an already-taken slot is refused | 409 slot_unavailable | 409 slot_unavailable | ✅ |
| **C2** | the race oracle itself: it CAN see more than one winner | 40 winners on 40 distinct slots | 40 winners, refusals {} | ✅ |
| **C3** | the schedule is enforced: a closed weekend offers no slots | 0 slots offered on a Saturday, from a well-formed 2xx | 0 slots offered (availability='ok') | ✅ |
| **C4** | the daily cap bites (max_per_day=2) | two created, the third refused (day_full) or no longer offered | 201/ok → 201/ok → no_slots_offered | ✅ |
| **C5** | the backlog metric is LIVE (stranded work is seen, and then cleared) | the operator surface goes unreachable with the worker; on restart the first reading exceeds the baseline, then returns to 0 | baseline due=0; surface unreachable during the outage: yes; first reading after restart=6 (oldest 30.9s); drained in 7.3s | ✅ |
| **C6** | the no-show guard: an appointment that has not ended cannot be a no-show | 409 not_ended | 409 not_ended | ✅ |
| **C7** | an idempotent cancel emits exactly ONE booking.cancelled webhook | exactly 1 booking.cancelled at the sink for this booking | {'booking.created': 1, 'booking.cancelled': 1} | ✅ |
| **C8** | a reschedule race leaves ONE successor, not two live appointments | exactly 1 successor survives | 1 active: ['d21e85b0-2cae-4783-bac4-195ce901fb70 confirmed'] | ✅ |
| **C9** | a cancel racing a reschedule never leaves TWO live appointments | at most 1 active booking survives | 0 active: none | ✅ |
| **C10** | RF-04 itself: N simultaneous bookings of ONE slot leave exactly one | exactly 1 winner of 40, the rest `slot_unavailable` | 1 winner(s), 39 refused {'slot_unavailable': 39} | ✅ |
| **C11** | the no-show transition itself, against a real end time | 200, and the booking's status really becomes no_show | 200 ok → status=no_show | ✅ |
| **C12** | the outbox actually drained, and the backlog was readable throughout | due == 0 before the run ends, with 0 unexplained scrape failures | drained=True after 0.0s; unexplained scrape failures=0 | ✅ |

## 6. Error taxonomy (every outcome, successes included)

Total classified responses: **520** · non-2xx: **35**

| status | machine code | count |
|---:|---|---:|
| 200 | `ok` | 278 |
| 201 | `ok` | 207 |
| 409 | `slot_unavailable` | 35 |

## 7. Limits — what this dataset does and does NOT support

### It DOES support these claims

These are **mechanical guarantees**: properties of the code under concurrency. A compressed simulation tests them at least as harshly as calendar time would — harder, in fact, because real traffic never collides on purpose.

- **No double-booking under simultaneous contention.** Barrier-released bursts at one slot left exactly one winner, and the distinct-slot control (C2) proves the oracle could have seen more than one.
- **Mutations are serialised, checked on the DIARY rather than on status codes.** Concurrent cancels of one booking emit exactly one `booking.cancelled` (C7); a reschedule race leaves exactly one successor (C8); a cancel racing a reschedule never leaves two live appointments (C9). ==The winner counts in §4 are not the oracle for the last two==: a cancel and a reschedule may BOTH legitimately answer 200, because the reschedule swaps in a successor and the cancel then finds the predecessor already cancelled and is an idempotent no-op. What must never happen is two live appointments, so that is what is asserted.
- **Latency under this load, on this hardware.** The distributions in §2, at the sample sizes shown there.
- **The drain keeps up, and a dead drain would be visible.** Peak backlog in §3, with the dead-man control (C5) proving the metric is live rather than a constant zero.
- **Both locales and several guest timezones exercise the same paths** without a distinct error class appearing in §6.

### It does NOT support these — by construction, not by omission

- ==**"≥5 bookings from real humans."** That is a fact about ADOPTION, and no volume of synthetic load can stand in for it.== Every booking here was made by this harness against a throwaway database. The rubric's human-uptake criterion is untouched by this run and must still be met by the pilot itself.
- ==**"Two weeks elapsed."** The run compresses two weeks of VOLUME into minutes; it does not compress two weeks of TIME.== Nothing here can see certificate expiry, log-rotation faults, a cron that drifts, a slow memory leak, connection churn over days, a 24h reminder actually firing, or a DST transition. Duration-dependent failure is what a soak test finds, and this is not one.
- **Real-world demand shape.** The weekday weights are plausible, not fitted — five pilot bookings cannot fit a distribution. Read §1 as a load shape, never as a forecast.
- **Real email deliverability.** Mail lands in Mailpit over plaintext SMTP on the same host. This says nothing about SPF/DKIM/DMARC, spam placement, or whether any real inbox would accept the message.
- **Real network conditions.** No reverse proxy, no TLS termination, no WAN latency, no CDN. Client and server share a host, so §2 is a floor for the product's own work — not what a guest in another city experiences.
- **The booking PAGE.** The harness drives the API. Front-end budgets (LCP/CLS/INP) and the browser journey are measured elsewhere (`e2e/`, the CWV guard) and are out of scope.
- **External calendar integrations.** No Google or CalDAV connection is configured, so the busy-cache refresh path and its degraded/unavailable modes are not exercised.
- **Payments.** No payment provider is configured; the money paths are untouched.
- **Multi-worker safety.** The stack runs the shipped `replicas: 1`. This says nothing about what two workers would do — which the product documents as unsafe by design.
- ==**A DEAD worker is not visible in these numbers.**== `/metrics/summary` is served BY the worker, so while the drain is down the backlog does not climb — the endpoint is unreachable (`Connection refused`). C5 confirms that directly. The backlog metric therefore detects a drain that is FALLING BEHIND, not one that is GONE; the second needs external liveness monitoring, and nothing in §3 substitutes for it.

### Honest reading of the sample sizes

The p99 figures in §2 rest on the counts in that table. A p99 over 239 observations is one or two slow requests, not a stable tail estimate; p50 and p95 are the trustworthy columns at this volume.
