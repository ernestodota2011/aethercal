{
  "name": "@aethercal/calendar-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "AetherCal calendar JS workspace: headless core + React layer, bundled for the Reflex wrapper.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:libs": "node build-libs.mjs",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "eslint .",
    "check-react": "node scripts/check-react-alignment.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "esbuild": "^0.28.1",
    "eslint": "^9.15.0",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.15.0"
  },
  "pnpm": {
    "overrides": {
      "vite": "^6.4.3"
    },
    "onlyBuiltDependencies": ["esbuild"]
  }
}
