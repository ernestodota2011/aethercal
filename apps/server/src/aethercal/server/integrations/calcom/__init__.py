"""cal.com compatibility layer.

AetherCal replaces the agency's cal.com; existing services consume cal.com's webhooks. This package
pins the webhook contract AetherCal must keep satisfying (see ``contract``); the F1 outgoing-webhook
emitter and the compatibility adapter build on it.
"""
